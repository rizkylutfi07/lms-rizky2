# API URL untuk server-side rendering (SSR) dan fallback dev
# Untuk AKSES LAN  → frontend otomatis pakai http://192.168.1.11:3001
# Untuk CLOUDFLARE → frontend otomatis pakai https://api.smkpgribanyuputih.cloud
# Deteksi runtime di lib/api.ts → resolveApiUrl()
NEXT_PUBLIC_API_URL=http://localhost:3001
